# device_api core has no third-party dependencies.
#
# 'cryptography' is needed only for AdminClient.change_secret() (AES-256-GCM
# of the new secret under the current one). Everything else -- discovery,
# ON/OFF/DIM, OTA, the keys-file store -- is pure stdlib.
cryptography>=42
